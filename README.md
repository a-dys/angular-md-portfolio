# Portfolio page based on Angular Material-Start (ES6 Tutorials)

To run:
- npm install
- sudo npm install jspm live-server -g
- jspm update
- live-server --open=app

To build prod:
- jspm bundle-sfx src/boot/boot
